(function (window, $, undefined) {
  'use strict';

  _531.Models.LiftRowModel = Backbone.Model.extend({

    defaults: {
      lift: ''
    },

    initialize: function (attributes) {
    }
  
  });

}(window, jQuery));
